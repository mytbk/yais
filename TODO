Add ncurses(dialog) UI
Add mount options support
LVM/btrfs subvol support
Unmount partitions on exit
Add clone-cd function
Add a lock to prevent multi-process
Add multi-lingual support
internal pacstrap,(arch-)chroot,genfstab
